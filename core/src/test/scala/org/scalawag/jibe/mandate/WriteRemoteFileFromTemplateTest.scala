package org.scalawag.jibe.mandate
/*
import java.io.File

import org.scalamock.scalatest.MockFactory
import org.scalatest.{FunSpec, Matchers}

class WriteRemoteFileFromTemplateTest extends FunSpec with WriteRemoteFileBaseTest with Matchers with MockFactory {
  val content = command.FileContent("hello")
  val template = command.FileContent("<%@ val word: String %>${word}")
  val args = Map("word" -> "hello")
  val destination = new File("/tmp/destination")

  generateTests(WriteRemoteFileFromTemplate(destination, template, args), content)
}
*/